import { ValidarViajeUsuario } from './validar-viaje-usuario';

describe('ValidarViajeUsuario', () => {
  it('should create an instance', () => {
    expect(new ValidarViajeUsuario()).toBeTruthy();
  });
});
